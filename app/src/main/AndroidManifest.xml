<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sy.whattoeat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatToEat">
        <activity android:name=".AddFoodActivity"></activity>
        <activity android:name=".EatOutFoodsActivity" />
        <activity android:name=".StatisticActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".DietActivity" />
        <activity android:name=".EatHomeActivity" />
        <activity android:name=".EatOutActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>